let array1 = [2, 4, 7, 1, 99];

// Sort the array in ascending order
array1.sort((a, b) => a - b);
console.log("Array in ascending order: ", array1);

// Sort the array in descending order
array1.sort((a, b) => b - a);
console.log("Array in descending order: ", array1);


//after geating the number by the user 
// Get the array of numbers from the user
// let array1 = prompt("Enter a list of numbers separated by commas: ").split(",").map(Number);

// // Sort the array in ascending order
// array1.sort((a, b) => a - b);
// console.log("Array in ascending order: ", array1);

// // Sort the array in descending order
// array1.sort((a, b) => b - a);
// console.log("Array in descending order: ", array1);