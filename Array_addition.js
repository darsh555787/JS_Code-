const array1 = [2, 4, 7, 1, 99];

// Calculate the sum of all elements in the array
const sum = array1.reduce((a, b) => a + b, 0);

console.log(`The sum of all elements in the array is: ${sum}`);



//get input from user and the addition of the array
// Get the array of numbers from the user
// let array1 = prompt("Enter a list of numbers separated by commas: ").split(",").map(Number);

// // Calculate the sum of all elements in the array
// const sum = array1.reduce((a, b) => a + b, 0);

// console.log(`The sum of all elements in the array is: ${sum}`);