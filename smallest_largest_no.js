const array = [2, 4, 7, 1, 99];

// Find the largest number in the array
const largest = Math.max(...array);

// Find the smallest number in the array
const smallest = Math.min(...array);

console.log(`The largest number in the array is: ${largest}`);
console.log(`The smallest number in the array is: ${smallest}`);


//Find the smallest number and the largest number  in the array
//input by the user

// Get the array of numbers from the user
// let array = prompt("Enter a list of numbers separated by commas: ").split(",").map(Number);

// // Find the largest number in the array
// const largest = Math.max(...array);

// // Find the smallest number in the array
// const smallest = Math.min(...array);

// console.log(`The largest number in the array is: ${largest}`);
// console.log(`The smallest number in the array is: ${smallest}`);